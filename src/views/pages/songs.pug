extends ../layouts/main-layout.pug

block title 
  title Songs

block content
  h1 Songs

  section
    each song in songs
      article(style="margin:1.2rem;padding:1.2rem;background-color:peru;")
        h3 #{song.title}
        p #{song.description}